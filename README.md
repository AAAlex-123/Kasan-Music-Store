# Kasan-Music-Store

### Generate the HTML files

Open a command prompt inside any of the `ExerciseX` directories and execute the following
command:

```bash
python ../preprocessor.py document/*
```

The generated .html files should appear in the `ExerciseX/` directory.
